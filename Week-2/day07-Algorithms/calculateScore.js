cont outOfTen = (score, totalScore) => {
    return score/totalScore*10
}

const itWorksforSixteenAndTwenty = (score, totalScore) => {return 8}

console.log('It works for 16 and 20', itWorksforSixteenAndTwenty())l